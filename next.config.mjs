/** @type {import('next').NextConfig} */
const nextConfig = {
  //By adding reactStrictMode: false the app will not make the http request twice
  //   reactStrictMode: false,
};

export default nextConfig;
